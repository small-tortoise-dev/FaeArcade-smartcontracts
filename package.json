{
  "name": "fae-ton",
  "version": "1.0.0",
  "description": "TON smart contracts monorepo using Tact and Blueprint",
  "private": true,

  "scripts": {
    "build": "tsx scripts/build.ts",
    "build:contracts": "tsx scripts/build.ts",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "deploy": "tsx scripts/deploy.ts",
    "deploy:testnet": "tsx scripts/deploy.ts --network testnet",
    "deploy:mainnet": "tsx scripts/deploy.ts --network mainnet",
    "test:treasury": "tsx scripts/testTreasury.ts",
    "script": "tsx",
    "clean": "rimraf build dist"
  },
  "devDependencies": {
    "@ton/blueprint": "^0.39.1",
    "@ton/sandbox": "^0.36.0",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "rimraf": "^5.0.0",
    "ton": "^6.13.1",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@tact-lang/compiler": "^1.6.13",
    "ton-core": "^0.53.0"
  },
  "packageManager": "pnpm@8.0.0"
}
